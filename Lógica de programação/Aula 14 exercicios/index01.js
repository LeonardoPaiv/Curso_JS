const maiorNumero = (n1, n2) => {
    const maiorNumero = Math.max(n1,n2);
    return maiorNumero
}

const max = (n1,n2) => n1 > n2 ? n1 : n2;

console.log(maiorNumero(10, 20));
console.log(max(5, 15));